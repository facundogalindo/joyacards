body {
  margin: 0;
  font-family: Arial, sans-serif;
  background: #f3f4f6;
  color: #111827;
}

.container {
  max-width: 900px;
  margin: 40px auto;
  padding: 20px;
}

.card {
  background: white;
  padding: 24px;
  margin-bottom: 24px;
  border-radius: 12px;
  box-shadow: 0 4px 14px rgba(0, 0, 0, 0.1);
}

h1,
h2,
h3,
p {
  margin-top: 0;
}

h1 {
  text-align: center;
  font-size: 48px;
  margin-bottom: 16px;
}

h2 {
  margin-bottom: 18px;
}

button {
  margin-top: 16px;
  padding: 10px 18px;
  border: none;
  border-radius: 8px;
  background: #2563eb;
  color: white;
  cursor: pointer;
  font-weight: bold;
}

button:hover {
  background: #1d4ed8;
}

button:disabled {
  background: #94a3b8;
  cursor: not-allowed;
}

.secondary-button {
  background: #6b7280;
}

.secondary-button:hover {
  background: #4b5563;
}

.clear-button {
  margin-left: 12px;
}

input {
  display: block;
  margin-top: 12px;
}

.field {
  display: block;
  margin-top: 16px;
  font-weight: bold;
}

.field input {
  width: 120px;
  padding: 8px;
  margin-top: 8px;
}

/* Pantalla inicial */

.welcome-card {
  text-align: center;
}

.welcome-image {
  width: 100%;
  max-width: 480px;
  display: block;
  margin: 0 auto 24px;
  border-radius: 16px;
}

/* Info superior del cuestionario */

.quiz-top-info {
  margin-bottom: 12px;
}

.file-used {
  font-size: 14px;
  color: #6b7280;
  margin-bottom: 12px;
  text-align: center;
}

.file-used strong {
  color: #111827;
}

.quiz-stats {
  display: flex;
  justify-content: space-between;
  gap: 12px;
  margin-bottom: 12px;
  padding: 10px 12px;
  background: #f3f4f6;
  border-radius: 8px;
  font-weight: bold;
  color: #374151;
}

.quiz-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.quiz-header p {
  margin-bottom: 6px;
}

.quiz-header button {
  margin-top: 0;
}

.question-type-badge {
  display: inline-block;
  margin-top: 4px;
  padding: 4px 10px;
  border-radius: 999px;
  background: #e0e7ff;
  color: #3730a3;
  font-size: 13px;
  font-weight: bold;
}

/* Imagen dentro de una pregunta */

.question-image {
  width: 100%;
  max-width: 420px;
  max-height: 320px;
  object-fit: contain;
  display: block;
  margin: 18px auto;
  border-radius: 12px;
  border: 1px solid #e5e7eb;
  background: #f9fafb;
}

/* Opciones normales */

.options {
  display: flex;
  flex-direction: column;
  gap: 12px;
  margin-top: 18px;
}

.option {
  padding: 12px;
  border: 1px solid #d1d5db;
  border-radius: 8px;
  background: #f9fafb;
}

.selectable {
  cursor: pointer;
  display: flex;
  align-items: center;
  gap: 10px;
}

.selectable input {
  margin: 0;
}

.option.selected {
  border-color: #2563eb;
  background: #eff6ff;
}

/* Varios desplegables */

.multi-dropdown-list {
  display: flex;
  flex-direction: column;
  gap: 14px;
  margin-top: 18px;
}

.multi-dropdown-item {
  padding: 14px;
  border: 1px solid #d1d5db;
  border-radius: 10px;
  background: #f9fafb;
}

.multi-dropdown-item p {
  margin-bottom: 10px;
}

.question-select {
  width: 100%;
  padding: 12px;
  border: 1px solid #d1d5db;
  border-radius: 8px;
  background: white;
  font-size: 16px;
}

.question-select:disabled {
  background: #e5e7eb;
  cursor: not-allowed;
}

/* Resultado */

.result {
  margin-top: 20px;
  padding: 16px;
  border-radius: 8px;
  background: #f3f4f6;
}

.correct-result {
  color: #166534;
  text-align: center;
}

.wrong-result {
  color: #991b1b;
}

.result-image {
  width: 140px;
  max-width: 100%;
  display: block;
  margin: 0 auto 12px;
  border-radius: 12px;
}

/* Utilidades viejas */

.actions {
  display: flex;
  gap: 12px;
  margin-top: 20px;
}

.json-preview {
  background: #111827;
  color: #e5e7eb;
  padding: 16px;
  border-radius: 8px;
  overflow-x: auto;
  font-size: 14px;
}

/* Responsive */

@media (max-width: 600px) {
  .container {
    margin: 20px auto;
    padding: 12px;
  }

  .card {
    padding: 18px;
  }

  h1 {
    font-size: 36px;
  }

  .quiz-stats {
    flex-direction: column;
    gap: 6px;
  }

  .quiz-header {
    align-items: flex-start;
    gap: 12px;
  }

  .question-image {
    max-height: 260px;
  }
}

/* Builder */

.builder-page {
  max-width: 1100px;
}

.builder-hero {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 24px;
}

.builder-hero h1 {
  text-align: left;
  margin-bottom: 4px;
  line-height: 1;
}

.builder-hero p {
  color: #4b5563;
  margin-top: 0;
  margin-bottom: 0;
  display: block;
}

.builder-card,
.builder-list-card {
  padding: 28px;
}

.builder-section-title {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  margin-bottom: 20px;
}

.builder-section-title h2 {
  margin-bottom: 0;
}

.editing-badge {
  padding: 6px 12px;
  border-radius: 999px;
  background: #fef3c7;
  color: #92400e;
  font-weight: bold;
  font-size: 13px;
}

.builder-grid {
  display: grid;
  grid-template-columns: 280px 1fr;
  gap: 18px;
  align-items: start;
}

.builder-field {
  display: flex;
  flex-direction: column;
  gap: 8px;
  font-weight: bold;
  color: #374151;
}

.builder-field-wide {
  grid-column: span 1;
}

.builder-field select,
.builder-field textarea {
  width: 100%;
  box-sizing: border-box;
  border: 1px solid #d1d5db;
  border-radius: 10px;
  padding: 12px;
  font-size: 15px;
  background: white;
  color: #111827;
}

.builder-field textarea {
  min-height: 92px;
  resize: vertical;
  font-family: Arial, sans-serif;
}

.builder-panel {
  margin-top: 22px;
  padding: 18px;
  border: 1px solid #e5e7eb;
  border-radius: 14px;
  background: #f9fafb;
}

.builder-panel h3 {
  margin-bottom: 14px;
}

.panel-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 12px;
  margin-bottom: 14px;
}

.panel-header h2,
.panel-header h3,
.panel-header p {
  margin-bottom: 0;
}

.panel-header p {
  color: #6b7280;
}

.true-false-options {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
}

.answer-pill {
  display: flex;
  align-items: center;
  gap: 8px;
  min-width: 150px;
  padding: 12px 16px;
  border: 1px solid #d1d5db;
  border-radius: 999px;
  background: white;
  cursor: pointer;
  font-weight: bold;
}

.answer-pill input {
  margin: 0;
}

.answer-pill.active {
  border-color: #2563eb;
  background: #eff6ff;
  color: #1d4ed8;
}

.option-editor-list,
.dropdown-editor-list {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.option-editor-row,
.dropdown-editor-row {
  display: grid;
  grid-template-columns: 44px 1fr 140px 100px;
  gap: 10px;
  align-items: center;
}

.dropdown-editor-row {
  grid-template-columns: 44px 1fr 150px 100px;
}

.option-letter {
  font-weight: bold;
  color: #4b5563;
}

.option-editor-row input,
.dropdown-editor-row input,
.dropdown-editor-row select {
  width: 100%;
  box-sizing: border-box;
  margin: 0;
  padding: 11px 12px;
  border: 1px solid #d1d5db;
  border-radius: 9px;
  background: white;
  font-size: 14px;
}

.correct-toggle {
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: 13px;
  font-weight: bold;
  color: #374151;
}

.correct-toggle input {
  margin: 0;
}

.icon-button,
.danger-button {
  margin-top: 0;
}

.danger-button {
  background: #dc2626;
}

.danger-button:hover {
  background: #b91c1c;
}

.builder-actions {
  display: flex;
  gap: 12px;
  margin-top: 22px;
}

.builder-actions-inline {
  display: flex;
  gap: 10px;
  flex-wrap: wrap;
}

.empty-state {
  padding: 28px;
  border: 1px dashed #cbd5e1;
  border-radius: 14px;
  background: #f8fafc;
  color: #64748b;
  text-align: center;
  font-weight: bold;
}

.question-list-scroll {
  max-height: 330px;
  overflow-y: auto;
  padding-right: 6px;
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.question-list-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 12px;
  padding: 14px;
  border: 1px solid #e5e7eb;
  border-radius: 14px;
  background: #ffffff;
}

.question-list-main {
  display: flex;
  align-items: center;
  gap: 14px;
  min-width: 0;
}

.question-number {
  width: 34px;
  height: 34px;
  border-radius: 999px;
  background: #2563eb;
  color: white;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  font-weight: bold;
  flex-shrink: 0;
}

.question-list-badge {
  display: inline-block;
  margin-bottom: 4px;
  padding: 3px 9px;
  border-radius: 999px;
  background: #e0e7ff;
  color: #3730a3;
  font-size: 12px;
  font-weight: bold;
}

.question-list-item h3 {
  margin-bottom: 0;
  font-size: 16px;
  color: #111827;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  max-width: 520px;
}

.question-list-actions {
  display: flex;
  gap: 8px;
  flex-shrink: 0;
}

.question-list-actions button {
  margin-top: 0;
}

.format-preview {
  margin-top: 18px;
  max-height: 280px;
  overflow: auto;
  background: #111827;
  color: #e5e7eb;
  padding: 16px;
  border-radius: 12px;
  font-size: 14px;
  white-space: pre-wrap;
}

/* Responsive builder */

@media (max-width: 800px) {
  .builder-hero {
    flex-direction: column;
    align-items: stretch;
  }

  .builder-grid {
    grid-template-columns: 1fr;
  }

  .option-editor-row,
  .dropdown-editor-row {
    grid-template-columns: 1fr;
  }

  .question-list-item {
    flex-direction: column;
    align-items: stretch;
  }

  .question-list-actions {
    justify-content: flex-end;
  }

  .question-list-item h3 {
    max-width: 100%;
    white-space: normal;
  }
}

.builder-import-card {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 22px;
}

.builder-import-card h2 {
  margin-bottom: 8px;
}

.builder-import-card p {
  color: #4b5563;
  margin-bottom: 0;
}

.import-button {
  position: relative;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-width: 150px;
  padding: 10px 18px;
  border-radius: 8px;
  background: #2563eb;
  color: white;
  font-weight: bold;
  cursor: pointer;
}

.import-button:hover {
  background: #1d4ed8;
}

.import-button input {
  display: none;
}

.correct-selected-label {
  color: #166534;
  font-size: 13px;
  font-weight: bold;
}

.correct-row {
  border-radius: 10px;
}

.correct-row input[type="text"],
.correct-row input:not([type]) {
  border-color: #22c55e;
  background: #f0fdf4;
}

@media (max-width: 800px) {
  .builder-import-card {
    flex-direction: column;
    align-items: stretch;
  }

  .import-button {
    width: 100%;
  }
}

.builder-image-panel {
  margin-top: 22px;
  padding: 18px;
  border: 1px solid #e5e7eb;
  border-radius: 14px;
  background: #f9fafb;
}

.builder-image-panel h3 {
  margin-bottom: 8px;
}

.builder-image-panel p {
  color: #4b5563;
  margin-bottom: 14px;
}

.builder-image-actions {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
}

.image-upload-button {
  margin-top: 0;
}

.image-upload-button input {
  display: none;
}

.builder-image-preview {
  margin-top: 14px;
  padding: 12px;
  border: 1px dashed #cbd5e1;
  border-radius: 12px;
  background: white;
  text-align: center;
}

.builder-image-preview img {
  max-width: 260px;
  max-height: 180px;
  object-fit: contain;
  display: block;
  margin: 0 auto;
}

.builder-image-preview span {
  color: #64748b;
  font-weight: bold;
}

.image-upload-card-button {
  width: 100%;
  max-width: 360px;
  display: flex;
  align-items: center;
  gap: 14px;
  padding: 16px 18px;
  border: 2px dashed #93c5fd;
  border-radius: 16px;
  background: linear-gradient(135deg, #eff6ff, #ffffff);
  color: #1e3a8a;
  cursor: pointer;
  box-sizing: border-box;
  transition: all 0.18s ease;
}

.image-upload-card-button:hover {
  border-color: #2563eb;
  background: linear-gradient(135deg, #dbeafe, #ffffff);
  transform: translateY(-2px);
  box-shadow: 0 8px 20px rgba(37, 99, 235, 0.18);
}

.image-upload-card-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  background: #2563eb;
  color: white;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 24px;
  flex-shrink: 0;
}

.image-upload-card-text {
  display: flex;
  flex-direction: column;
  gap: 3px;
}

.image-upload-card-text strong {
  font-size: 16px;
}

.image-upload-card-text small {
  font-size: 13px;
  color: #64748b;
}

.image-upload-card-button input {
  display: none;
}

/* ========================= */
/* Responsive mobile general */
/* ========================= */

@media (max-width: 768px) {
  body {
    background: #f3f4f6;
  }

  .container {
    width: 100%;
    max-width: 100%;
    margin: 0;
    padding: 12px;
    box-sizing: border-box;
  }

  .card {
    width: 100%;
    box-sizing: border-box;
    padding: 18px;
    margin-bottom: 16px;
    border-radius: 14px;
  }

  h1 {
    font-size: 34px;
    line-height: 1.1;
  }

  h2 {
    font-size: 22px;
    line-height: 1.25;
  }

  h3 {
    font-size: 18px;
  }

  button {
    width: 100%;
    margin-top: 12px;
    padding: 12px 16px;
    font-size: 15px;
  }

  .secondary-button,
  .danger-button {
    width: 100%;
  }

  .clear-button {
    margin-left: 0;
  }

  input,
  select,
  textarea {
    max-width: 100%;
    box-sizing: border-box;
  }

  .field input {
    width: 100%;
  }
}

/* ========================= */
/* Pantalla inicial mobile */
/* ========================= */

@media (max-width: 768px) {
  .welcome-image {
    max-width: 100%;
    border-radius: 14px;
  }

  .welcome-card {
    padding: 16px;
  }
}

/* ========================= */
/* Simulacro mobile */
/* ========================= */

@media (max-width: 768px) {
  .quiz-stats {
    flex-direction: column;
    align-items: stretch;
    gap: 6px;
    font-size: 15px;
  }

  .quiz-header {
    flex-direction: column;
    align-items: stretch;
    gap: 12px;
  }

  .quiz-header button {
    width: 100%;
  }

  .file-used {
    text-align: left;
    font-size: 13px;
  }

  .question-type-badge {
    font-size: 12px;
  }

  .options {
    gap: 10px;
  }

  .option {
    padding: 14px;
    font-size: 16px;
  }

  .selectable {
    align-items: flex-start;
  }

  .selectable input {
    margin-top: 3px;
    flex-shrink: 0;
  }

  .question-image {
    max-width: 100%;
    max-height: 260px;
    margin: 14px auto;
  }

  .result {
    padding: 14px;
  }

  .result-image {
    width: 120px;
  }

  .multi-dropdown-item {
    padding: 12px;
  }

  .question-select {
    font-size: 15px;
    padding: 12px;
  }
}

/* ========================= */
/* Builder mobile */
/* ========================= */

@media (max-width: 768px) {
  .builder-page {
    max-width: 100%;
  }

  .builder-hero {
    flex-direction: column;
    align-items: stretch;
    gap: 14px;
  }

  .builder-hero h1 {
    font-size: 32px;
    text-align: left;
  }

  .builder-import-card {
    flex-direction: column;
    align-items: stretch;
  }

  .import-button {
    width: 100%;
    box-sizing: border-box;
  }

  .builder-grid {
    grid-template-columns: 1fr;
  }

  .builder-field textarea {
    min-height: 100px;
  }

  .builder-panel {
    padding: 14px;
  }

  .panel-header {
    flex-direction: column;
    align-items: stretch;
  }

  .option-editor-row,
  .dropdown-editor-row {
    grid-template-columns: 1fr;
    gap: 8px;
    padding: 12px;
    border: 1px solid #e5e7eb;
    border-radius: 12px;
    background: white;
  }

  .option-letter {
    font-size: 15px;
  }

  .correct-toggle {
    justify-content: flex-start;
  }

  .true-false-options {
    flex-direction: column;
  }

  .answer-pill {
    width: 100%;
    box-sizing: border-box;
  }

  .builder-actions {
    flex-direction: column;
  }

  .builder-actions-inline {
    flex-direction: column;
    width: 100%;
  }

  .builder-image-actions {
    flex-direction: column;
  }

  .image-upload-button {
    width: 100%;
    box-sizing: border-box;
  }

  .builder-image-preview img {
    max-width: 100%;
    max-height: 220px;
  }

  .question-list-scroll {
    max-height: 360px;
  }

  .question-list-item {
    flex-direction: column;
    align-items: stretch;
  }

  .question-list-main {
    align-items: flex-start;
  }

  .question-list-actions {
    flex-direction: column;
  }

  .question-list-item h3 {
    max-width: 100%;
    white-space: normal;
    overflow: visible;
    text-overflow: unset;
  }

  .format-preview {
    font-size: 13px;
    max-height: 260px;
  }
}

/* ========================= */
/* Extra small phones */
/* ========================= */

@media (max-width: 420px) {
  .container {
    padding: 8px;
  }

  .card {
    padding: 14px;
    border-radius: 12px;
  }

  h1 {
    font-size: 30px;
  }

  h2 {
    font-size: 20px;
  }

  .option {
    font-size: 15px;
  }

  .quiz-stats {
    font-size: 14px;
  }

  .question-image {
    max-height: 220px;
  }
}

/* ========================= */
/* Builder - opciones nuevas */
/* ========================= */

.option-card {
  padding: 14px;
  border: 1px solid #d1d5db;
  border-radius: 14px;
  background: #ffffff;
}

.option-card-correct {
  border-color: #22c55e;
  background: #f0fdf4;
}

.option-card-title {
  margin: 0 0 10px 0;
  font-size: 15px;
  font-weight: bold;
  color: #374151;
  text-align: left;
}

.option-card input {
  width: 100%;
  box-sizing: border-box;
  margin: 0;
  padding: 12px 14px;
  border: 1px solid #d1d5db;
  border-radius: 10px;
  background: white;
  font-size: 15px;
}

.option-card-correct input {
  border-color: #22c55e;
  background: #f0fdf4;
}

.option-card-actions {
  display: flex;
  gap: 10px;
  align-items: center;
  margin-top: 12px;
}

.correct-check-button {
  margin-top: 0;
  flex: 1;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  background: #f8fafc;
  color: #374151;
  border: 1px solid #cbd5e1;
  border-radius: 10px;
  padding: 12px 14px;
  font-weight: bold;
}

.correct-check-button:hover {
  background: #eef2f7;
}

.correct-check-button.active {
  background: #dcfce7;
  border-color: #22c55e;
  color: #166534;
}

.check-icon {
  font-size: 18px;
  font-weight: bold;
  line-height: 1;
}

/* Mobile */
@media (max-width: 768px) {
  .option-card {
    padding: 14px;
  }

  .option-card-title {
    font-size: 15px;
    margin-bottom: 10px;
  }

  .option-card-actions {
    flex-direction: row;
    gap: 8px;
  }

  .correct-check-button {
    flex: 1;
    width: auto;
    margin-top: 0;
    padding: 12px;
    font-size: 14px;
  }

  .danger-button {
    flex: 1;
    width: auto;
    margin-top: 0;
  }
}

@media (max-width: 480px) {
  .option-card-actions {
    flex-direction: row;
    align-items: stretch;
  }

  .correct-check-button,
  .danger-button {
    font-size: 14px;
    padding: 12px 10px;
  }

  .check-icon {
    font-size: 17px;
  }
}

/* ========================= */
/* Ajuste visual builder opciones */
/* ========================= */

.panel-helper-text {
  margin-top: 4px;
  color: #6b7280;
  font-size: 14px;
}

.option-editor-list {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.option-card {
  padding: 14px;
  border: 1px solid #d1d5db;
  border-radius: 14px;
  background: #ffffff;
  transition: 0.2s ease;
}

.option-card-correct {
  border-color: #22c55e;
  background: #f0fdf4;
  box-shadow: 0 0 0 1px rgba(34, 197, 94, 0.08);
}

.option-card-title {
  margin: 0 0 10px 0;
  font-size: 15px;
  font-weight: 700;
  color: #374151;
  text-align: left;
}

.option-card input {
  width: 100%;
  box-sizing: border-box;
  margin: 0;
  padding: 12px 14px;
  border: 1px solid #d1d5db;
  border-radius: 10px;
  background: white;
  font-size: 15px;
}

.option-card-correct input {
  border-color: #22c55e;
  background: #f8fff9;
}

.option-card-actions {
  display: flex;
  justify-content: flex-end;
  gap: 10px;
  margin-top: 12px;
}

.option-icon-button {
  margin-top: 0;
  width: 46px;
  height: 46px;
  min-width: 46px;
  padding: 0;
  border-radius: 12px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  font-size: 20px;
  font-weight: bold;
}

.correct-icon-button {
  background: #f8fafc;
  color: #64748b;
  border: 1px solid #cbd5e1;
}

.correct-icon-button:hover {
  background: #eef2f7;
}

.correct-icon-button.active {
  background: #dcfce7;
  color: #15803d;
  border: 1px solid #22c55e;
}

.delete-icon-button {
  background: #fee2e2;
  color: #dc2626;
  border: 1px solid #fecaca;
}

.delete-icon-button:hover {
  background: #fecaca;
}

/* Mobile */
@media (max-width: 768px) {
  .option-card {
    padding: 14px;
  }

  .option-card-title {
    font-size: 15px;
    margin-bottom: 10px;
  }

  .option-card-actions {
    justify-content: flex-end;
  }

  .option-icon-button {
    width: 44px;
    height: 44px;
    min-width: 44px;
    font-size: 18px;
  }
}